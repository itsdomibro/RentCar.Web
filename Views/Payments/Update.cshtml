@model RentCar.Web.ViewModels.PaymentController.UpdatePaymentViewModel

@{
    ViewData["Title"] = "Update Payment";
}

<div class="page-container">
    <div class="page-header">
        <div>
            <h1 class="page-title">Update Payment</h1>
            <p class="page-subtitle">Edit payment information</p>
        </div>
        <a asp-action="Index" class="btn btn-outline-secondary">Back to List</a>
    </div>

    <div class="view-card">
        <form asp-action="Update" method="post" class="view-form" asp-antiforgery="true">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
            <input type="hidden" asp-for="PaymentId" />

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="Date" class="form-label">Date</label>
                    <input asp-for="Date" class="form-control" type="date" />
                    <span asp-validation-for="Date" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Amount" class="form-label">Amount</label>
                    <div class="input-group">
                        <span class="input-group-text">$</span>
                        <input asp-for="Amount" class="form-control" />
                    </div>
                    <span asp-validation-for="Amount" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="PaymentMethod" class="form-label">Payment Method</label>
                <input asp-for="PaymentMethod" class="form-control" />
                <span asp-validation-for="PaymentMethod" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="RentalId" class="form-label">Rental ID</label>
                <input asp-for="RentalId" class="form-control" />
                <span asp-validation-for="RentalId" class="text-danger"></span>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Update</button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
